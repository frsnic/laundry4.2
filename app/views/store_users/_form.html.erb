<%= simple_form_for(@store_user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :store_id %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
